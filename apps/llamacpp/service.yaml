apiVersion: v1
kind: Service
metadata:
  name: llamacpp
  labels:
    app: llamacpp
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 8080
      targetPort: http
      protocol: TCP
  selector:
    app: llamacpp
