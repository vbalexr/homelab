apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
metadata:
  name: immich
  namespace: immich

resources:
  - service.yaml
  - deployment-server.yaml
  - deployment-ml.yaml
  - configmap.yaml

