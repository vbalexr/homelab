apiVersion: v1
kind: Service
metadata:
  name: chilu-bb
  labels:
    app: chilu-bb
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: chilu-bb
