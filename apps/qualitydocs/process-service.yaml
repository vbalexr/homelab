apiVersion: v1
kind: Service
metadata:
  name: process-service
  labels:
    app: process-service
spec:
  type: ClusterIP
  selector:
    app: process-service
  ports:
    - name: http
      port: 3002
      targetPort: http
      protocol: TCP
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: process-service
  labels:
    app: process-service
spec:
  replicas: 1
  selector:
    matchLabels:
      app: process-service
  template:
    metadata:
      labels:
        app: process-service
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 10001
        fsGroup: 10001
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: process-service
          image: ghcr.io/vbalexr/qualitydocs-process-service:latest
          imagePullPolicy: IfNotPresent
          securityContext:
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          ports:
            - name: http
              containerPort: 3002
              protocol: TCP
          env:
            - name: NODE_ENV
              value: production
            - name: PORT
              value: "3002"
            - name: SERVICE_NAME
              value: process-service
            - name: DB_SCHEMA
              value: process
          envFrom:
            - secretRef:
                name: qualitydocs-secret
