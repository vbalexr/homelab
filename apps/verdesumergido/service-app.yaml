apiVersion: v1
kind: Service
metadata:
  name: verdesumergido-app
  labels:
    app: verdesumergido
    component: app
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: verdesumergido
    component: app
