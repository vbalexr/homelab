apiVersion: v1
kind: Service
metadata:
  name: verdesumergido
  labels:
    app: verdesumergido
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 80
      targetPort: http
      protocol: TCP
  selector:
    app: verdesumergido
