apiVersion: v1
kind: Service
metadata:
  name: verdesumergido-api
  labels:
    app: verdesumergido
    component: api
spec:
  type: ClusterIP
  ports:
    - name: http
      port: 3001
      targetPort: http
      protocol: TCP
  selector:
    app: verdesumergido
    component: api
