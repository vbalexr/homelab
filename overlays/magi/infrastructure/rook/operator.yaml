apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: rook-ceph
  namespace: rook-ceph
spec:
  chart:
    spec:
      chart: rook-ceph
      version: "v1.19.3" # Use your preferred version
      sourceRef:
        kind: HelmRepository
        name: rook-release
        namespace: rook-ceph
  interval: 1h
  install:
    createNamespace: true
