apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - ../../../../apps/valkey
  - secret.yaml

patches:
  - path: statefulset-patch.yaml
    target:
      group: apps
      version: v1
      kind: StatefulSet
      name: valkey
  - path: service-patch.yaml
    target:
      version: v1
      kind: Service
      name: valkey
