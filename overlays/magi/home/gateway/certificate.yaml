apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: home-tls
  namespace: home
spec:
  secretName: home-tls
  issuerRef:
    name: letsencrypt-prod
    kind: ClusterIssuer
  dnsNames:
    - photos.vbalex.com
    - vault.vbalex.com
