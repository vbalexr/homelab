apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: home-gateway
spec:
  gatewayClassName: cilium
  infrastructure:
    labels:
      pool: public
  listeners:
    # Omit listener hostname so both assistant.vbalex.com and cameras.vbalex.com
    # routes can attach (hostname on listener intersects with route hostnames).
    - name: http
      protocol: HTTP
      port: 80
      allowedRoutes:
        namespaces:
          from: Same
    - name: https
      protocol: HTTPS
      port: 443
      tls:
        mode: Terminate
        certificateRefs:
          - kind: Secret
            name: home-gateway-tls
      allowedRoutes:
        namespaces:
          from: Same
