apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: frigate-http-redirect
spec:
  parentRefs:
    - name: home-gateway
      sectionName: http
  hostnames:
    - cameras.vbalex.com
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
