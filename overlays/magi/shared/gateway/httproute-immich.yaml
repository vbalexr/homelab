apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: immich-route
  namespace: shared
spec:
  parentRefs:
    - name: shared-gateway
      sectionName: https
  hostnames:
    - photos.vbalex.com
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
        - name: immich
          port: 2283
